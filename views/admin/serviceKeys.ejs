<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Сервисные ключи</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
   <header>
    <div class="header-container">
      <h1><a href="/admin">Главная</a></h1><h2>Сервисные ключи</h2>
      <div class="user-info">
        Пользователь: <%= user.username %> |
        <a href="/logout">Выйти</a>
      </div>
    </div>
  </header>
  <main>
    <div class="actions">
      <a href="/admin/service-keys/new" class="btn">Добавить ключ</a>
    </div>
    <table>
      <thead>
        <tr>
          <th>Код</th>
          <th>Описание</th>
          <th>Создан</th>
          <th>Использований</th>
          <th>Действия</th>
        </tr>
      </thead>
      <tbody>
        <% keys.forEach(k => { %>
          <tr>
            <td><%= k.code %></td>
            <td><%= k.description || '-' %></td>
            <td><%= k.createdAt.toLocaleString() %></td>
            <td><%= k.usageCount %></td>
            <td>
              <a href="/admin/service-keys/<%= k._id %>/logs" class="btn">Логи</a>
              <form action="/admin/service-keys/<%= k._id %>/delete" method="POST" style="display:inline">
                <button class="btn btn--danger">Удалить</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </main>
</body>
</html>
